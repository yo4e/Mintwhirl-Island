# Game Plan: Pocket Storm — Solo Battle Royale

## Risk Tasks

### 1. ポインターロックを用いる三人称カメラと照準
- **Why isolated:** ブラウザのポインターロックはユーザー操作を要し、埋め込み環境で失敗した際にもゲーム開始導線を壊さない設計が必要です。視点回転とプレイヤー移動の座標系がずれるとTPSとして成立しません。
- **Approach:** 開始ボタンのクリックで入力を明示的に有効化し、マウス差分でカメラの方位・仰角を更新します。WASD はカメラの水平前方・右方向を基準に変換し、追従カメラはプレイヤーの背後上方に固定距離で追尾します。ポインターロックが利用できない場合はクリック中のドラッグ回転を代替として残します。
- **Verify:** 開始後にマウス移動で視点だけが回転し、W/S はカメラの向く水平前後、A/D は左右へ移動すること。プレイヤーがカメラの前方を背にし続け、照準が画面中央に保たれること。

### 2. NPCの追跡・射撃・安全地帯への退避
- **Why isolated:** 完全な経路探索は小規模デモの範囲を越えるため、障害物があるアリーナでも破綻しにくい軽量な行動選択が必要です。
- **Approach:** 各NPCに「巡回・接敵・射撃・嵐から退避」の明示的な状態を持たせます。直線追跡を基本とし、近距離の障害物には横方向の回避ベクトルを足します。安全リング外ではリング中心へ向かう優先度を上げ、プレイヤー射線に入った時だけクールダウン制のヒットスキャンを行います。
- **Verify:** NPC がプレイヤーを認識すると接近し、射程内で停止して射撃すること。リング外では内側へ移動すること。岩や小屋に接触しても長時間停止せず、被弾・撃破時にHPと残敵数が正しく変わること。

### 3. ヒットスキャン射撃、投射表示、ダメージ判定
- **Why isolated:** 高速な射撃で見た目と当たり判定がずれると、TPSの手応えが損なわれます。
- **Approach:** 画面中央からのレイを利用して最初の命中対象を決め、短命の弾道ラインと命中時の紙吹雪型マーカーを表示します。プレイヤーの武器には弾数・リロード・連射間隔を持たせ、NPCにも同じHPインターフェースを使います。
- **Verify:** 中央照準に敵を捉えてクリックすると弾数が一つ減り、可視の弾道とヒットマーカーが現れ、敵HPが減ること。弾切れからリロードへ移行し、補給箱で弾薬が増えること。敵を倒すと残敵数が減ること。

## Main Build

明るい低ポリゴンの島を一枚のアリーナとして作ります。プレイヤーはジャケットとバックパックを持つデフォルメ冒険者、NPCは黄色・ライラック・コーラルの配色で区別されたロボット風シルエットです。小屋、岩、丸い木、風車、橋、川、補給箱を配置し、地形の起伏と障害物を使って遮蔽と移動経路を作ります。開始画面で操作を確認し、ゲーム中は左下のHP、上中央の嵐タイマー、右下の弾薬・リロード状態、右上の簡易ミニマップを表示します。

- **Assets:**
  - `pocket-storm-logo` — UIと開始画面に使用する透明ロゴ。
  - `pocket-storm-terrain` — 地面のマテリアルとして反復使用する草地テクスチャ。
  - `pocket-storm-sky` — 開始画面とゲーム環境を繋ぐ空のビジュアル指標。
  - `pocket-storm-supply-crate` — 補給箱の配色・装飾参照。実体は軽量な立方体メッシュで構成。
  - `pocket-storm-visual-target` — HUD密度、カメラ距離、色彩、地形構成の視覚基準。
- **Verify:**
  - プレイヤー入力が移動・照準・射撃・リロードと一致すること。
  - プレイヤーとNPCが地面・岩・建物を貫通しないこと。
  - HUDが可読性を保ち、狭い画面でも主要数値が重ならないこと。
  - 開始画面、戦闘、勝利、敗北、再挑戦の一連がプレイできること。
  - 生成済み画像の色調、カメラ高さ、可愛く明瞭なローポリ密度を維持すること。
  - 主要アセットに破損URLやブラウザコンソールエラーがないこと。
  - `?demo` で決定論的な自動プレイ状態を表示し、スクリーンショットで戦闘の進行が読めること。

## Productization Pass — Pocket Storm: Island Run

### プレイの柱

1. **毎ラウンド異なる小さなビルド**：撃破の節目で3択の「ポケット強化」を選び、火力・生存・機動を自分の好みに寄せる。選択中は戦闘を停止し、読みやすい玩具カードで効果を見せる。
2. **危険と回収の天秤**：倒したNPCのドロップ、終盤の補給イベント、嵐の圧力を同時に置き、「今すぐ安全地帯へ行くか」「もう一つ回収するか」を毎回発生させる。
3. **終盤の見せ場**：嵐レベル3でエリート敵を出現させ、レベル4ではセンター付近に緊急補給を落とす。生き残るだけでなく、危険を越えた報酬を見せる。
4. **短いが残る達成感**：スコア、島バッジ、累計バッジをリザルトに記録し、次回の到達目標を明示する。ブラウザでは `localStorage` で小さな進行を保持する。

### 今回の実装スコープ

| システム | 実装内容 | 検証基準 |
| --- | --- | --- |
| ポケット強化 | 2撃破ごとに3択を提示。火力・ホップ・回復・弾薬などから選ぶ。 | 選択で戦闘が一時停止し、効果がHUD・数値へ反映される。 |
| 島バッジ | 撃破と生存時間からスコアを算出し、ラウンド終了時に累計バッジへ加算する。 | 勝敗後に今回・累計・次の目標が表示される。 |
| エリート／緊急補給 | 嵐レベル3で高HPのエリート、レベル4でセンター補給を発生させる。 | 戦闘中にイベント告知が表示され、普段と異なる回収判断が生まれる。 |
| 商品品質の導線 | 日本語中心のチュートリアル、イベント表示、リザルトを用意する。 | 初見のプレイヤーが目的・強化・操作・次の目標を読み取れる。 |
